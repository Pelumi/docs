---
title: 'Fetch bank codes'
description: 'Fetch the necessery bank codes and names of your desired banks'
---

<Card title="Fetch bank codes and names" icon="money-bill"
href="/nomba-api-reference/transfers/fetch-bank-codes-and-names">
  Fetch the bank codes and names tied to all banks
</Card>

# `POST /v1/transfers/banks`

#### Response body

<ResponseField name="code" type="string" required>
  Response code
</ResponseField>

<ResponseField name="description" type="string" required>
  Response description
</ResponseField>

<ResponseField name="data" type="object">
    <ResponseField name="results" type="object[]">
        Contains result of all banks fetched.
        <Expandable title="object"  defaultOpen="true">
            <ResponseField name="code" type="string">
                The bank's code.
            </ResponseField>
            <ResponseField name="name" type="string">
                The bank's name.
            </ResponseField>
            
        </Expandable>
    </ResponseField>
</ResponseField>

<RequestExample>
```bash Request
curl --request GET \
  --url https://api.nomba.com/v1/transfers/banks \
  --header 'Authorization: Bearer <token>' \
  --header 'accountId: <accountid>'
```
</RequestExample>

<ResponseExample>
```json Success response
{
  "code": "00",
  "description": "Success",
  "data": {
    "results": [
      {
        "code": "058",
        "name": "Guaranty Trust Bank"
      },
      {
        "code": "090160",
        "name": "Addosser Microfinance Bank"
      },
    ]
  }
}
```
</ResponseExample>